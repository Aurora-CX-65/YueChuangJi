# 阅创集 (YueChuangJi) —— 从零开始部署指南

> 📖 **想快速上手？** 你正在看的就是零基础版。  
> 📋 **需要完整技术参考？** 请查看 [DOCKER.md](./DOCKER.md)（含架构图、环境变量完整说明、管理命令速查）。

---

## 目录

- [基础概念：2 分钟搞懂 Docker 是什么](#基础概念2-分钟搞懂-docker-是什么)
- [场景一：在自己电脑上跑起来（5 分钟）](#场景一在自己电脑上跑起来5-分钟)
- [场景二：部署到云服务器，让全世界访问（30 分钟）](#场景二部署到云服务器让全世界访问30-分钟)
- [场景三：绑定域名 + 开启 HTTPS 小锁（额外 15 分钟）](#场景三绑定域名--开启-https-小锁额外-15-分钟)
- [日常维护：更新、备份、查看日志](#日常维护更新备份查看日志)
- [救命手册：遇到报错怎么办？](#救命手册遇到报错怎么办)
- [名词解释](#名词解释)

---

## 基础概念：2 分钟搞懂 Docker 是什么

### 打个比方

> 你把整个网站打包成一个"集装箱"（Docker 镜像），  
> 然后在任何电脑上"卸货"（运行容器），  
> 网站就能立刻跑起来。

- **不需要**安装 Java、Node.js、MySQL、Redis 这些乱七八糟的东西
- **不需要**配置环境变量、改一堆文件
- 你在自己电脑上怎么跑，服务器上就怎么跑——**完全一样**

### 你需要安装的唯一东西

| 你的电脑 | 下载地址 |
|----------|---------|
| Windows | [Docker Desktop for Windows](https://www.docker.com/products/docker-desktop/) |
| Mac (Intel) | [Docker Desktop for Mac (Intel)](https://www.docker.com/products/docker-desktop/) |
| Mac (M1/M2/M3) | [Docker Desktop for Mac (Apple Silicon)](https://www.docker.com/products/docker-desktop/) |
| Linux 服务器 | 见下方说明 ↓ |

> **Linux 服务器一键安装 Docker**（在服务器终端里粘贴下面两行命令）：  
> ```bash
> curl -fsSL https://get.docker.com | bash
> sudo systemctl enable docker && sudo systemctl start docker
> ```

安装完成后，打开终端（Windows 打开 PowerShell 或 Git Bash），输入：

```bash
docker --version
```

如果看到 `Docker version 2x.x.x`，恭喜，安装成功！

---

## 场景一：在自己电脑上跑起来（5 分钟）

> 适合：想在本地测试功能、熟悉操作流程。

### 第一步：获取项目代码

```bash
# 把这行命令复制到终端里执行
git clone --recurse-submodules https://github.com/Aurora-CX-65/YueChuangJi.git
cd YueChuangJi
```

> `git clone` = 从 GitHub 下载项目代码到你的电脑  
> `--recurse-submodules` = 同时下载前端和后端的代码（它们是独立仓库）

### 第二步：复制一份配置文件

```bash
# Windows 用户 (PowerShell):
copy .env.docker .env

# Mac / Linux 用户:
cp .env.docker .env
```

> 这步做了什么：`.env.docker` 是一个"配置模板"，复制成 `.env` 后，系统会自动读取它。  
> **本地测试不需要改任何内容**，直接用默认值就好。

### 第三步：一键启动

```bash
docker compose up -d --build
```

> `docker compose up` = 启动所有服务  
> `-d` = 后台运行（不会占着你的终端窗口）  
> `--build` = 自动编译代码  

**首次启动需要下载和编译，大约 5-10 分钟。** 你会看到终端在滚动各种输出，这是正常的。

### 第四步：打开浏览器访问

| 页面 | 地址 |
|------|------|
| 网站首页 | [http://localhost](http://localhost) |
| 接口文档 | [http://localhost/swagger-ui/index.html](http://localhost/swagger-ui/index.html) |
| 健康检查 | [http://localhost:8080/actuator/health](http://localhost:8080/actuator/health) |

> `localhost` = "我自己的电脑"

### 🎉 恭喜！网站已经在你电脑上跑起来了！

---

## 场景二：部署到云服务器，让全世界访问（30 分钟）

> 适合：想把网站发布到公网，任何人都能通过 IP 地址访问。

### 🛒 第一步：买一台云服务器（如果还没有）

以下三个平台选一个（新人都有优惠，几十块钱一年）：

| 平台 | 选购指南 |
|------|---------|
| [阿里云 ECS](https://www.aliyun.com/product/ecs) | 选"突发性能实例"，2核4G，CentOS 7.9 或 Ubuntu 22.04 |
| [腾讯云 CVM](https://cloud.tencent.com/product/cvm) | 选"轻量应用服务器"，2核4G，Ubuntu 22.04 |
| [华为云 ECS](https://www.huaweicloud.com/product/ecs.html) | 选"弹性云服务器"，2核4G，Ubuntu 22.04 |

> 📌 **关键配置清单**：
> - CPU：2 核
> - 内存：4 GB（最低 2 GB 也能跑）
> - 硬盘：40 GB 以上
> - 系统：**Ubuntu 22.04**（推荐新手用这个）
> - 带宽：3 Mbps 起步

购买后你会得到：
- **公网 IP**：类似 `123.45.67.89` 的一串数字
- **root 密码**：登录服务器的密码

### 🔌 第二步：连接到你的服务器

**Windows 用户**：下载 [MobaXterm](https://mobaxterm.mobatek.net/)（免费，推荐）或者用系统自带的 PowerShell：

```powershell
ssh root@你的服务器IP
```

**Mac / Linux 用户**：直接打开终端：

```bash
ssh root@你的服务器IP
```

> 第一次连接会问 `Are you sure you want to continue connecting?`  
> 输入 `yes` 然后回车。  
> 然后输入 root 密码（**输入密码时看不到任何字符，这是正常的，输完回车即可**）。

### 🐳 第三步：在服务器上安装 Docker

连上服务器后，依次执行以下命令：

```bash
# 1. 安装 Docker（一行命令搞定）
curl -fsSL https://get.docker.com | bash

# 2. 设置 Docker 开机自启动
sudo systemctl enable docker
sudo systemctl start docker

# 3. 验证安装成功
docker --version
# 应该显示: Docker version 2x.x.x
```

### 📥 第四步：下载项目代码

```bash
# 安装 git（如果服务器没有）
sudo apt update && sudo apt install git -y

# 下载项目
git clone --recurse-submodules https://github.com/Aurora-CX-65/YueChuangJi.git

# 进入项目目录
cd YueChuangJi
```

### ⚙️ 第五步：配置你的网站

```bash
# 复制配置模板
cp .env.docker .env

# 用 nano 编辑器打开配置文件
nano .env
```

> `nano` 是 Linux 上一个简单的文本编辑器。  
> 用方向键移动光标，修改内容，按 `Ctrl+O` 保存，按 `Ctrl+X` 退出。

**你只需要改以下 4 行：**

```env
# 把 localhost 改成你的服务器公网 IP（例如 123.45.67.89）
PUBLIC_DOMAIN=123.45.67.89

# 把 localhost 改成带 IP 的完整地址
FILE_ACCESS_BASE_URL=http://123.45.67.89

# ⚠️ 改一个复杂的密码！不要用默认值！
DB_ROOT_PASSWORD=MyStr0ng!Passw0rd#2024

# ⚠️ 改一个复杂的 Redis 密码！不要用默认值！
REDIS_PASSWORD=An0therStr0ng!Passw0rd
```

> 📌 **其他配置项说明**（不改也没关系）：
> - `DEEPSEEK_API_KEY`：如果要使用 AI 写作功能才需要填
> - `MAIL_HOST / MAIL_USERNAME / MAIL_PASSWORD`：如果要发邮件（注册验证、通知）才需要填
> - `ENABLE_SSL`：先保持 `false`，后面有域名了再改

保存退出（`Ctrl+O` → 回车 → `Ctrl+X`）。

### 🔥 第六步：开放防火墙端口

你的服务器有一个"防火墙"，默认会挡住外部访问。需要放行 80 端口。

```bash
# Ubuntu / Debian 系统（最常见）
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw enable

# CentOS 系统
sudo firewall-cmd --add-port=80/tcp --permanent
sudo firewall-cmd --add-port=443/tcp --permanent
sudo firewall-cmd --reload
```

> ⚠️ **重要！** 还要去云服务商的控制台放行端口：  
> 登录阿里云/腾讯云/华为云后台 → 找到"安全组"或"防火墙" → 添加规则：  
> - 端口：`80`（HTTP）
> - 端口：`443`（HTTPS）
> - 授权对象：`0.0.0.0/0`
>
> 这一步非常容易漏掉，漏了的话网站会打不开！

### 🚀 第七步：启动网站

```bash
docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build
```

> 首次启动大约需要 5-10 分钟（编译代码 + 下载数据库 + 初始化数据）。  
> 可以另外开一个终端窗口查看进度：
> ```bash
> docker compose logs -f
> ```
> 当你看到 `Started YueChuangJiApiApplication` 时，后端就启动完成了。

### ✅ 第八步：验证是否成功

在浏览器地址栏输入：`http://你的服务器IP`

> 例如：`http://123.45.67.89`

如果看到了阅创集的页面——**🎉 恭喜，你的网站已经上线了！**

---

### 💡 换个 IP 就部署好了？跟传统方式比省了多少事？

| 传统部署方式 | Docker 方式 |
|-------------|------------|
| 安装 Java 17 | ❌ 不需要 |
| 安装 Maven | ❌ 不需要 |
| 安装 Node.js 22 | ❌ 不需要 |
| 安装 MySQL 8.0 + 配置 | ❌ 不需要 |
| 安装 Redis + 配置 | ❌ 不需要 |
| 安装 Nginx + 配置反向代理 | ❌ 不需要 |
| 配置环境变量 | ❌ 不需要（.env 一个文件搞定） |
| 手动初始化数据库 | ❌ 不需要（自动建表 + 导入数据） |
| 编译后端 Jar 包 | ❌ 不需要 |
| 编译前端 dist | ❌ 不需要 |
| 处理跨域 CORS | ❌ 不需要 |
| **总计：一条命令搞定** | ✅ `docker compose up -d --build` |

---

## 场景三：绑定域名 + 开启 HTTPS 小锁（额外 15 分钟）

> 适合：已经买了域名，想让网站用 `https://你的域名.com` 访问，浏览器显示🔒小锁。

### 🌐 第一步：配置域名解析

去你的域名服务商（阿里云/腾讯云/GoDaddy 等）后台，添加 DNS 解析：

| 记录类型 | 主机记录 | 记录值 |
|----------|---------|--------|
| A | @ | 你的服务器 IP |
| A | www | 你的服务器 IP |

> 解析生效需要 1-10 分钟（最长 24 小时）。

### ⚙️ 第二步：修改配置

```bash
cd ~/YueChuangJi
nano .env
```

修改以下内容：

```env
# 改成你的域名
PUBLIC_DOMAIN=yuechuangji.com

# 协议改成 https，地址改成域名
PROTOCOL=https
FILE_ACCESS_BASE_URL=https://yuechuangji.com

# SSL 先保持 false（等获取证书后再改）
ENABLE_SSL=false
```

保存退出（`Ctrl+O` → 回车 → `Ctrl+X`）。

### 🚀 第三步：先用 HTTP 模式启动

```bash
docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build
```

启动完成后，用 `http://你的域名.com` 确认能访问。

### 🔒 第四步：获取免费 SSL 证书

```bash
# 1. 先停掉 Nginx（释放 80 端口给证书工具用）
docker compose -f docker-compose.yml -f docker-compose.prod.yml stop frontend

# 2. 获取证书（把下面的域名和邮箱换成你自己的）
bash setup-ssl.sh yuechuangji.com admin@yuechuangji.com

# 3. 如果第 2 步报错，试试这个备选方案：
#    docker run --rm -v "$(pwd)/docker/nginx/ssl:/etc/letsencrypt" -p 80:80 \
#      certbot/certbot certonly --standalone --agree-tos \
#      -m admin@yuechuangji.com -d yuechuangji.com
```

> 成功后，你会在 `docker/nginx/ssl/` 目录下看到两个文件：
> - `fullchain.pem`（证书文件）
> - `privkey.pem`（私钥文件）
>
> Let's Encrypt 是免费 SSL 证书，有效期 90 天，会自动续期。

### 🔓 第五步：开启 HTTPS

```bash
# 编辑配置
nano .env

# 把 ENABLE_SSL 改成 true
ENABLE_SSL=true

# 保存退出后，重新构建启动
docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build
```

### ✅ 第六步：验证 HTTPS

在浏览器输入 `https://你的域名.com`，地址栏左边应该出现 🔒 小锁。

### 🔄 第七步：设置证书自动续期

```bash
# 添加定时任务，每天凌晨 3 点自动检查并续期
(crontab -l 2>/dev/null; echo "0 3 * * * docker run --rm -v $(pwd)/docker/nginx/ssl:/etc/letsencrypt -p 80:80 certbot/certbot renew --quiet && docker compose -f $(pwd)/docker-compose.yml -f $(pwd)/docker-compose.prod.yml restart frontend") | crontab -

# 查看是否添加成功
crontab -l
```

> 看到刚才那行命令就说明定时任务设置好了。以后证书会自动续期，不需要你操心。

---

## 日常维护：更新、备份、查看日志

### 🔄 更新网站代码

当 GitHub 上有新版本时：

```bash
cd ~/YueChuangJi

# 拉取最新代码
git pull
git submodule update --remote

# 重新构建并重启
docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build
```

### 💾 备份数据库

```bash
# 备份到当前目录，文件名自动带日期
docker compose exec mysql mysqldump -uroot -p yuechuangji > backup_$(date +%Y%m%d).sql

# 提示输入密码时，输入你在 .env 里设置的 DB_ROOT_PASSWORD
```

> 建议定期把这个 `.sql` 文件下载到自己电脑上保存。

### 📊 查看系统状态

```bash
docker compose ps                 # 看哪些服务在运行
docker compose stats              # 看内存和 CPU 使用情况
docker compose logs -f --tail=50  # 看最近 50 行日志
docker compose logs -f backend    # 只看后端的日志
```

### 🔄 启动 / 停止 / 重启

```bash
# 停止（不删数据）
docker compose -f docker-compose.yml -f docker-compose.prod.yml down

# 启动（数据还在）
docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d

# 重启（不重新构建，快）
docker compose -f docker-compose.yml -f docker-compose.prod.yml restart

# ⚠️ 停止并删除所有数据（数据库清空！）
docker compose -f docker-compose.yml -f docker-compose.prod.yml down -v
```

---

## 救命手册：遇到报错怎么办？

### ❌ 报错：`docker: command not found`

**原因**：Docker 没装。  
**解决**：回到场景二的第三步，重新安装 Docker。

---

### ❌ 报错：`port is already allocated` 或 `bind: address already in use`

**原因**：80 端口被其他程序占用了。  
**解决**：

```bash
# 查看谁占用了 80 端口
sudo lsof -i :80

# 如果是 nginx 或 apache 占用的，停掉它：
sudo systemctl stop nginx
sudo systemctl stop apache2

# 如果是其他程序，记住 PID（进程 ID），然后：
sudo kill -9 <PID>
```

---

### ❌ 报错：`Cannot connect to the Docker daemon`

**原因**：Docker 服务没启动。  
**解决**：

```bash
sudo systemctl start docker
```

---

### ❌ 网站打开了但是白屏，或者一直转圈

**可能原因 1**：后端还没启动完成。

```bash
# 等 2 分钟再试，或者查看后端日志：
docker compose logs -f backend
# 看到 "Started YueChuangJiApiApplication" 就说明好了
```

**可能原因 2**：防火墙没放行端口。  
回到场景二的第六步，检查安全组配置。

---

### ❌ 浏览器显示"无法访问此网站"

**排查步骤**：

1. **确认服务器在运行**  
   能连上 SSH 就说明服务器正常。

2. **确认 Docker 容器在运行**  
   ```bash
   docker compose ps
   ```
   所有服务状态都应该是 `Up`（不是 `Exit`）。

3. **确认防火墙放行了 80 端口**  
   参考场景二的第六步。

4. **确认云服务商安全组放行了 80 端口**  
   这一步最容易忘！

---

### ❌ 数据库连接失败 / 用户登录不了

```bash
# 查看后端日志中的错误信息
docker compose logs backend | grep -i error

# 常见原因：数据库密码不对，检查 .env 中的 DB_ROOT_PASSWORD
```

---

### ❌ git clone 提示连接失败

**原因**：网络问题或 GitHub 访问慢。  
**解决 1**：多试几次。  
**解决 2**：使用代理。  
**解决 3**：手动下载 ZIP 包上传到服务器。

---

### ❌ `docker compose up` 卡住不动

**原因**：首次构建需要下载大量依赖，网络慢时会很久。  
**解决**：耐心等待，或者试试换个时间（国内网络晚上可能比较慢）。

---

### 🆘 终极方案：全部重来

如果实在搞不定，清空一切从头开始：

```bash
cd ~/YueChuangJi
docker compose -f docker-compose.yml -f docker-compose.prod.yml down -v   # 删除所有容器和数据
docker system prune -af                                                    # 清理 Docker 缓存
rm -rf ~/YueChuangJi                                                      # 删除项目文件夹
```

然后从场景二的第四步重新开始。

---

## 名词解释

> 遇到陌生词汇？来这查一下。

| 名词 | 大白话解释 |
|------|-----------|
| **Docker** | 一个工具，能把网站打包成"集装箱"，到处都能跑 |
| **镜像 (Image)** | 集装箱里的"货物清单"——网站运行需要的一切 |
| **容器 (Container)** | 正在运行的"集装箱"——一个活着的网站实例 |
| **docker compose** | 一键命令，同时启动 4 个容器 |
| **Nginx** | 一个高性能的网页服务器，负责把页面发给浏览器 |
| **SSL / HTTPS** | 浏览器地址栏的 🔒 小锁，加密传输，防窃听 |
| **Let's Encrypt** | 免费提供 SSL 证书的公益组织 |
| **端口 (Port)** | 数字门牌号。80=网页，443=加密网页，3306=数据库 |
| **.env** | 配置文件，存密码、API Key 等设置 |
| **MySQL** | 数据库，存用户信息、文章内容等 |
| **Redis** | 高速缓存，让网站更快 |
| **容器编排** | 安排哪个容器先启动，哪个后启动 |
| **Volume（数据卷）** | Docker 的"U 盘"，容器删了数据不会丢 |

---

## 附录：推荐的服务器配置

| 用途 | 配置 | 月费参考 |
|------|------|---------|
| 个人测试 | 1核 2G | ~¥30 |
| 小团队使用 | 2核 4G | ~¥60 |
| 正式运营 | 4核 8G | ~¥150 |

> 先从 2核4G 开始，不够可以随时升级（云服务器都支持弹性扩容）。

---

## 需要帮助？

- 📧 联系开发者：3180303647@qq.com
- 🐛 提交 Issue：[GitHub Issues](https://github.com/Aurora-CX-65/YueChuangJi/issues)
- 📖 API 文档：[http://你的域名/swagger-ui/index.html](http://你的域名/swagger-ui/index.html)
- 📋 完整技术文档：[DOCKER.md](./DOCKER.md)
